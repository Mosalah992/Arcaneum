# Tasks

1. Confirm the current workspace remote/branch sync evidence and record that the repo tracks `origin/master` already.
2. Add the OpenSpec change folder and artifacts.
3. Investigate the markdown seed generator and the catalogue/reader row rendering path.
4. Patch the generator or content metadata so the cataloguing UI receives the correct volume metadata for the reported examples.
5. Regenerate the SQL seed and run the relevant verification command (`npm run typecheck` or `npm run build`) with proof of fresh output.
